<Page x:Class="TRPO12_2.Pages.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:TRPO12_2.Pages"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
            DataContext="{Binding RelativeSource={RelativeSource Self}}"
      Title="MainPage"
            Background="White">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal">
            <Button Margin="5" Padding="5" Click="go_form">Добавить</Button>
            <Button Margin="5" Padding="5" Click="Button_Click_1">Удалить</Button>
            <Button Margin="5" Padding="5" Click="go_roles">Роли</Button>
            <Button Margin="5" Padding="5" Click="go_group">Группы</Button>
        </StackPanel>
        <ListView Grid.Row="1" ItemsSource="{Binding service.Polzovats}" MouseDoubleClick="Edit"
SelectedItem="{Binding polzovat}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Margin="5">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Идентификатор: "/>
                            <TextBlock Text="{Binding Id}"/>
                        </StackPanel>
                        <TextBlock>
                <Run Text="Роль: "/>
                <Run Text="{Binding Role.Title}"/></TextBlock>

                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Логин: "/>
                            <TextBlock Text="{Binding Login}"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Имя: "/>
                            <TextBlock Text="{Binding Name}"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Почта: "/>
                            <TextBlock Text="{Binding Email}"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Пароль: "/>
                            <TextBlock Text="{Binding Password}"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Дата и время создания пользователя: "/>
                            <TextBlock Text="{Binding CreateAt, StringFormat='{}{0:dd.MM.yyyy HH:mm:ss}'}"/>
                        </StackPanel>
                        <TextBlock>Паспорт</TextBlock>
                        <TextBlock>
                    <Run Text="Серия: "/>
                    <Run Text="{Binding Passport.Series}"/>
                    <Run Text="Номер: "/>
                    <Run Text="{Binding Passport.Number}"/>
                        </TextBlock>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
